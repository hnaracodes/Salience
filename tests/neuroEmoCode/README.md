# NeuroEmo tests (archived)

Run explicitly when working on supervised training:

```powershell
python -m pytest tests/neuroEmoCode/ -v --tb=short
```

Excluded from default `pytest tests/` via `tests/conftest.py`.
